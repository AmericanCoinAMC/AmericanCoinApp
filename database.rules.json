{
    "rules": {
        ".read": "true",
        ".write": "auth != null",

        //Users Related
        "users": {
            ".indexOn": ["isAdmin", "email"]
        },

        //Posts Related
        "posts": {
            ".indexOn": ["url", "timestamp"]
        },
        "sourcePosts": {
            "$sourceKey": {
                "$categoryKey": {
                    ".indexOn": ["timestamp"]
                }
            }
        },
        "categoryPosts": {
            "$categoryKey": {
                ".indexOn": ["timestamp"]
            }
        },
        "outstandingPosts": {
            ".indexOn": ["timestamp"]
        },
        "hashtagPosts": {
            "$hashtagKey": {
                ".indexOn": ["timestamp"]
            }
        },
        "leaderPosts": {
            "$leaderKey": {
                ".indexOn": ["timestamp"]
            }
        },
        "pendingPosts": {
            ".indexOn": ["url", "timestamp"]
        },
        "userPendingPosts": {
            "$userKey": {
                ".indexOn": ["timestamp"]
            }
        },

        //Hashtags Related
        "hashtags": {
            ".indexOn": ["timestamp"]
        },

        //Sources Related
        "sources": {
            ".indexOn": ["timestamp", "status/active"]
        },

        //Leaders Related
        "leaders": {
            ".indexOn": ["timestamp"]
        },

        //Orion Related
        "orionConsole": {
            ".indexOn": ["timestamp", "type"]
        }

    }
}